-- ============================================================================
-- unj / unj-reze DB統合: game_players を撤去
--
-- ゴーストプレイヤーの座標（x/y/emoji）を2秒間隔で書き込むプレゼンス情報を
-- Neon に持たせていた。Postgres は本来こういう高頻度・使い捨てデータ向きではなく、
-- リアルタイムハブ（Koyeb、メモリ上）が既に同じ役割を持っている
-- （REALTIME_URL 設定時はそちらしか使わない設計だった）。
-- ハブ未設定時のフォールバックとして残っていたが、フォールバック自体を廃止し、
-- ハブが無ければプレゼンス機能を出さない方針にした
-- （components/LiveGameView.tsx / lib/db/interface.ts / lib/db/pg.ts 参照）。
--
-- 中身は15秒でTTL切れになる使い捨てデータなので、消えて困るものは無い。
--
-- unj のDB（NEON_DATABASE_URL）に対して流す。
-- ============================================================================
DROP TABLE IF EXISTS game_players;
